import { Text } from '@mantine/core';
import { AppLayout } from '@/components/AppLayout';

export const App = () => {
  return (
    <AppLayout>
      <Text>App content</Text>
    </AppLayout>
  );
};
