import { Button } from '@mantine/core';
import { Link } from 'react-router-dom';

export const Login = () => (
  // TODO: add login form + auth
  <Link to="/home">
    <Button>Login</Button>
  </Link>
);
